services:
  app:
    build: .
    ports:
      - "3010:3010"
    volumes:
      - .:/app
      - ./notes:/notes
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - NOTES_PATH=/notes
    stdin_open: true
    tty: true
